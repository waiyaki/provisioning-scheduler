// Currently have to have inline styles since material UI will not
// accept css modules.

const titleStyles = {
  fontSize: '2em'
};

const cardTextStyles = {
  fontSize: '1.2em'
};

const notRegistered = {
  fontSize: '0.8em',
  marginBottom: '0.6em'
};

export default {
  titleStyles,
  cardTextStyles,
  notRegistered
};
